# SPDX-FileCopyrightText: 2023-2024 City of Espoo
#
# SPDX-License-Identifier: LGPL-2.1-or-later

version: '3.5'

services:
  service-tests:
    image: 095341522062.dkr.ecr.eu-north-1.amazonaws.com/oppivelvollisuus/service-builder:${TAG:-master}
    build:
      context: ../service/
      target: builder
    ports:
      - "8080:8080"
    environment:
      JAVA_OPTS: -server -Djava.security.egd=file:/dev/./urandom -Xms1024m -Xss512k -Xmx1024m -XX:TieredStopAtLevel=1
      APP_DATABASE_URL: jdbc:postgresql://oppivelvollisuus-db:5432/oppivelvollisuus_it
      APP_DATABASE_USERNAME: oppivelvollisuus
      APP_DATABASE_PASSWORD: postgres
      APP_DATABASE_FLYWAY_USERNAME: oppivelvollisuus
      APP_DATABASE_FLYWAY_PASSWORD: postgres
    entrypoint: ./gradlew test
    depends_on:
      - oppivelvollisuus-db
