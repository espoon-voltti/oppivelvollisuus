# SPDX-FileCopyrightText: 2023-2024 City of Espoo
#
# SPDX-License-Identifier: LGPL-2.1-or-later

app:
  database:
    url: jdbc:postgresql://localhost:5432/oppivelvollisuus_it
    username: oppivelvollisuus
    password: postgres
    flyway:
      username: oppivelvollisuus
      password: postgres
  jwt:
    public_keys_url: classpath:jwks.json
  async_job_runner:
    disable_runner: true
  integration:
    valpas:
      enabled: true
      opintopolku_base_url: http://mock-valpas.invalid
      username: test-user
      password: test-pass
      kunta_oid: 1.2.3.4

spring:
  flyway:
    # In integration tests, Flyway is run manually and not via Spring Boot
    enabled: false
  main:
    allow-bean-definition-overriding: true
